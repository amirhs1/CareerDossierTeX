% resume-link-decoration-screen.tex — the `screen' half of the link-decoration
% extraction pair (issue #278). Identical body, one option changed, and a rule
% drawn under the \href anchor text. Its baselines must match the `print' half
% exactly; `diff' between the two committed files is the check.
\documentclass[medium=screen, fontsize=10pt, margin=narrow]{careerdossier-resume}
\input{resume-link-decoration.inc}
