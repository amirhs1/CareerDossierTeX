<!-- tokenfixture-arch-notable.md — the tables the lint reads are absent.

Drives the third state (docs/TESTING.md, "A guard answers three states, not
two"). The file is a plausible architecture document and the headings it
carries are near-misses of the real ones, which is how this failure actually
arrives: a heading renamed in a documentation tidy-up. Reporting "no
mismatches" here would be the lint quietly retiring itself. -->

#### The type scale

Renamed, so the parser cannot find it.

#### Vertical rhythm and spacing

Renamed too.

#### Dimensions outside the vertical rhythm and page geometry

And renamed again.
