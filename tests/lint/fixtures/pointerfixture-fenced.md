# Fenced

A pointer inside fenced code is illustration, not a pointer, and must not be
resolved:

```text
`pointerfixture-nowhere.md` ("No Such Heading At All")
```

`pointerfixture-target.md` ("Verification") outside the fence still is one.
