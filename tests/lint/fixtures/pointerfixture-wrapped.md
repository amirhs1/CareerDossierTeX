# Wrapped

The pointer below straddles a newline, which is how most of them are written at
80 columns and the case a line-based scan silently skips:

`pointerfixture-target.md`
("Verification")

and one whose quoted name itself is split across the wrap:
`pointerfixture-target.md` ("Project field
values")
