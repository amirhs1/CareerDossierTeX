% tokenfixture-manual-notable.tex — the fontsize table is absent.
%
% Drives the third state. The subsubsection is there and reads as authoritative
% about sizes; only the table the lint parses has gone, which is what a rewrite
% into prose leaves behind. Reporting "no mismatches" here would be the lint
% quietly retiring itself.
\subsubsection{\texttt{fontsize}}
\label{sec:fontsize}

\opt{fontsize} selects one whole-point type scale for the whole document. The
sizes it resolves to are stated in \S\ref{sec:tokens} and no longer here.

\subsubsection{\texttt{margin}}

Prose after it.
