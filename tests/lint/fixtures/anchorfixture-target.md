# Anchor lint fixture target

A file the other fixtures link into. It is never scanned directly; it exists so
their links have something real to resolve against.

## Boundary ownership

Text.

### `careerdossier-tokens.sty`

A heading with backticks and a dot, whose anchor drops both.

## [0.8.0] - 2026-08-12

A release heading, the shape that broke three CHANGELOG.md links.

## Dvořák profile

A heading with a non-ASCII letter, which the derivation must keep.

## XeLaTeX → LuaLaTeX

Punctuation removal must leave two hyphens here, not one.

## A heading — with an em dash

The same case with the other mark.
