# Second name

One parenthetical, two names. The first resolves and the second does not — the
case a checker that reads only the first name reports as clean:

`pointerfixture-target.md` ("Verification" and "No Such Section")
