%% lintfixture-complete.sty — option-lint fixture (issue #233)
%%
%% Both options are complete. Expected verdict: OK for `fontsize' and `margin'.
%% This fixture is lint input only; it is never compiled and is not part of the
%% Work (see tests/lint/fixtures/README.md).

\msg_new:nnnn { lintfixture-complete } { unknown-fontsize }
  { Unknown~'fontsize'~value~'#1'~for~lintfixture-complete. }
  { The~accepted~values~are~'10pt',~'11pt',~and~'12pt'. }

\msg_new:nnnn { lintfixture-complete } { unknown-margin }
  { Unknown~'margin'~value~'#1'~for~lintfixture-complete. }
  { The~accepted~values~are~'normal'~and~'narrow'. }

\keys_define:nn { cdossier / lintfixture }
  {
    fontsize .choices:nn =
      { 10pt , 11pt , 12pt }
      { \tl_set_eq:NN \l__lintfixture_fontsize_tl \l_keys_choice_tl } ,
    fontsize / unknown .code:n =
      { \msg_error:nnn { lintfixture-complete } { unknown-fontsize } {#1} } ,
    fontsize .value_required:n = true ,
    margin .choices:nn =
      { normal , narrow }
      { \tl_set_eq:NN \l__lintfixture_margin_tl \l_keys_choice_tl } ,
    margin / unknown .code:n =
      { \msg_error:nnn { lintfixture-complete } { unknown-margin } {#1} } ,
    margin .value_required:n = true ,
  }
