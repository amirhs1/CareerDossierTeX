%% lintfixture-gamma — lint input for tests/lint/run-version-declarations.sh.
%% Never compiled; not part of the Work.
%% Declares itself as lintfixture-gama: the version and date agree with the
%% other two sources, so only the name argument is wrong.
\NeedsTeXFormat{LaTeX2e}
\ProvidesExplClass {lintfixture-gama} {2026-01-01} {1.2.3}
