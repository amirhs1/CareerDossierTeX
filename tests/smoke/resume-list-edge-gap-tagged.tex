% resume-list-edge-gap-tagged.tex -- see list-edge-gap.inc (issues #176, #191,
% #193). The tagged build takes a separate list-key path, because LaTeX Lab's
% itemize emulation cannot accept enumitem's `*' leftmargin calculation and
% because its block templates do not read `\@topsepadd', so it needs its own
% coverage of the list-edge contract at both ends.
\DocumentMetadata{tagging=on}
\documentclass{careerdossier-resume}
\input{list-edge-gap.inc}
