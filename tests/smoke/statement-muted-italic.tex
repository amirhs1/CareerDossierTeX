% statement-muted-italic.tex — smoke fixture: the statement context line is the
% de-emphasis role's second call site, so the non-default muted=italic must
% build cleanly there too (issues #271, #324). Both context fields are set so
% the line is emitted.
\documentclass[type=research, muted=italic]{careerdossier-statement}
\CDossierSetup{
  name        = {Ada Lovelace},
  email       = {ada@example.test},
  affiliation = {Example University},
}
\CDossierStatementSetup{
  application-context = {Faculty application},
  application-id      = {APP-144},
}
\begin{document}
\MakeCDossierStatementHeader
\end{document}
