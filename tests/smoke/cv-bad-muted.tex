% cv-bad-muted.tex — smoke fixture: an unsupported `muted' value must be
% rejected with a message naming the accepted values (issues #212, #271).
% `grey' is the plausible wrong spelling, so the fixture doubles as the record
% that the accepted spelling is `gray'.
%
% The résumé had this fixture from #271 and the CV did not, so the CV's `muted'
% rejection path was the one choice-valued option on a record class with no
% smoke coverage. Each class owns its own message naming its own class, so the
% résumé fixture could not stand in for it.
\documentclass[muted=grey]{careerdossier-cv}
\CDossierSetup{ name = {Ada Lovelace}, email = {ada@example.com} }
\begin{document}
\MakeCDossierHeader
\end{document}
