%% careerdossier-biblatex.sty
%% Part of the CareerDossierTeX project.
%%
%% Copyright (C) 2026 Amir Sadeghi.
%%
%% This work may be distributed and/or modified under the conditions of the
%% LaTeX Project Public License, either version 1.3c of this license or (at
%% your option) any later version. The latest version of this license is in
%%   https://www.latex-project.org/lppl.txt
%% and version 1.3c or later is part of all distributions of LaTeX version
%% 2008 or later.
%%
%% This work has the LPPL maintenance status `maintained'.
%% The Current Maintainer of this work is Amir Sadeghi.
%%
%% This work consists of the files listed in manifest.txt.
%%
%% careerdossier-biblatex is the opt-in boundary around BibLaTeX and Biber. It
%% owns the fixed academic bibliography profile and preferred-author emphasis;
%% CV geometry, manual publications, and resource selection remain elsewhere.

\NeedsTeXFormat{LaTeX2e}[2022-06-01]
\ProvidesExplPackage {careerdossier-biblatex} {2026-08-04} {0.7.0}
  {CareerDossierTeX optional BibLaTeX and Biber integration}

\msg_new:nnnn { careerdossier-biblatex } { missing-dependency }
  {
    The~optional~dependency~'biblatex'~was~not~found;~install~BibLaTeX~and~
    Biber~or~use~CDossierPublications.
  }
  {
    Install~BibLaTeX~and~Biber,~then~build~with~latexmk;~or~omit~
    \c_backslash_str usepackage\c_left_brace_str careerdossier-biblatex\c_right_brace_str
    ,~then~use~CDossierPublications~for~manual~entries.
  }
\file_if_exist:nF { biblatex.sty }
  { \msg_fatal:nn { careerdossier-biblatex } { missing-dependency } }

\RequirePackage { careerdossier-theme }
\RequirePackage
  [
    backend    = biber ,
    style      = numeric ,
    sorting    = ydnt ,
    hyperref   = true ,
    giveninits = true ,
  ]
  { biblatex }

%% Track the CV's calibrated list-item gap so the bibliography's inter-entry
%% spacing matches the CV's other lists at the same fontsize. Fall back to the
%% v0.2 fixed value when no CareerDossierTeX token system is loaded (for
%% example, careerdossier-biblatex used with a non-CareerDossierTeX class).
\cs_if_exist:NTF \CDossierRecordItemSepSkip
  { \setlength { \bibitemsep } { \CDossierRecordItemSepSkip } }
  { \setlength { \bibitemsep } { 6pt } }

%% Track the shared list label separation for the horizontal gap too, so an
%% entry number sits as close to its entry as a manual CDossierPublications
%% label does. BibLaTeX's own default is \biblabelsep = 2\labelsep, which lands
%% the gap at roughly one em -- and one em is exactly where pdftotext's default
%% (non-layout) mode stops reading the numbers as labels and starts reading
%% them as a separate left-hand column, emitting them as a block ahead of the
%% entry text (issue #199). Measured on tests/bibliography/cv-bibliography.tex
%% with Poppler 26.07.0, the fixture splits at a 11.955pt gap and pairs at
%% 11.905pt; the entries are correctly placed and `pdftotext -layout' reports
%% them in source order either way, so this is the extractor's column
%% heuristic rather than a content-stream defect.
%%
%% \CDossierListLabelSep is half the body size, so the gap clears that
%% threshold by a factor of two at every supported fontsize instead of sitting
%% on it. Fixing this horizontally is deliberate: #196 tied the *vertical*
%% inter-entry gap to \CDossierRecordItemSepSkip, and reopening a vertical gap
%% to appease an extractor would undo that calibration.
\cs_if_exist:NT \CDossierListLabelSep
  { \setlength { \biblabelsep } { \CDossierListLabelSep } }

%% Remove the stretch BibLaTeX puts at each URL breakpoint. \biburlbigskip is
%% 0mu plus 3mu by default -- one sixth of an em per breakpoint -- and
%% \biburlsetup copies it into \Urlmuskip, so a justified line ending in a URL
%% spreads the URL rather than the word spaces. Poppler splits a word wherever
%% the gap exceeds 0.1em, so a stretched URL extracts as
%% `https : / / example . invalid /', which is neither searchable nor copyable.
%% This is the only site in the project with stretchable glue inside a URL: the
%% contact line measures each item in its own box, and body text goes through
%% \CDossierLink, whose break points carry a penalty and no glue.
%%
%% Capping the stretch was the earlier fix (0mu plus 1mu, issue #199), and it
%% was a mitigation rather than a guarantee for a reason that turned out to be
%% reachable with an ordinary address: TeX exceeds a stated stretch to set an
%% otherwise underfull line, so a long enough URL split anyway. A 262-character
%% query-string URL in an @online entry, at the CV's own fontsize=12pt,
%% margin=normal, extracted as `silencing + diasporic + futurism' with the cap
%% in place (issue #312). Rigid glue is the guarantee the cap was not: TeX
%% cannot stretch what has no stretch component, at any measure or length.
\muskip_set:Nn \biburlbigskip { 0mu }

%% Rigid glue on its own moves the failure rather than removing it. url.sty
%% breaks a URL only at punctuation, so a query string's long `+'-joined runs
%% leave breakpoints ~10 characters apart; with no stretch to absorb the
%% remainder, TeX set the line 5.09pt and 4.79pt into the margin instead. The
%% missing ingredient is breakpoint density, exactly as in issue #308, where
%% \CDossierLink extends \UrlBreaks with the letters and digits so a body-text
%% address can break anywhere. BibLaTeX exposes the same capability for its own
%% URLs through three penalties, all 0 (disabled) by default. Raising them lets
%% a URL break inside a run of letters or digits when punctuation will not do,
%% which is what removes the overfull lines. As with \UrlBreaks, these break
%% points insert a penalty and no discretionary hyphen, so a wrapped address
%% still concatenates back exactly -- the copy-paste invariant of issue #294.
%%
%% 9000 ranks an intra-run break below url.sty's punctuation break points,
%% which cost \UrlBreakPenalty (100), so where both would fit the line TeX
%% prefers the punctuation. Do not read more into the value than that: with
%% rigid glue a line has no way to absorb a shortfall, so badness decides
%% almost every break and the penalty rarely gets a vote. Measured on
%% tests/bibliography/cv-bibliography.tex, 100 and 9000 chose the identical
%% break; what is not interchangeable is zero, which is BibLaTeX's default and
%% brings the overfull lines back.
%%
%% One consequence is visible and intended: URLs now break where the line ends
%% rather than at the nearest earlier punctuation, so an address can wrap
%% mid-run (`.../visib' + `le-url'). That is what filling the measure honestly
%% looks like. The tidier punctuation break it replaces was not free -- it was
%% bought by stretching the URL to justify the short line, which is the defect.
%%
%% The cost is in the log, not on the page: a line holding nothing but URL has
%% no glue to justify with, so TeX reports it underfull and sets it
%% ragged-right. That is the same warning \CDossierLink already produces for a
%% long body-text address (tests/links/resume-body-link.tex), and the same
%% trade -- a few points of white at the right edge, against an address that
%% pastes.
\setcounter { biburlnumpenalty } { 9000 }
\setcounter { biburlucpenalty  } { 9000 }
\setcounter { biburllcpenalty  } { 9000 }

%% The trailing space is inside the measured label box and does not widen the
%% gap; \biblabelsep above is what separates the label from the entry.
\DeclareFieldFormat { labelnumberwidth } { \mkbibbold {#1) } }

%% Show only the strongest public identifier: DOI, then e-print, then URL.
\renewbibmacro* { doi+eprint+url }
  {
    \iffieldundef { doi }
      {
        \iffieldundef { eprint }
          { \usebibmacro { url+urldate } }
          { \usebibmacro { eprint } }
      }
      { \printfield { doi } }
    \newunit \newblock
  }

%% Every link a bibliography entry emits displays its own address -- a DOI, an
%% e-print identifier, a URL -- so none of them is the undecorated-anchor-text
%% problem that issue #278 addresses, and the body-copy underline must not reach
%% them. It would also undo the work directly above: ulem breaks an underlined
%% run at its spaces only, so an underlined address would become one
%% unbreakable box and the intra-URL break points calibrated here would never
%% be offered to TeX at all.
%%
%% \AtBeginBibliography's code runs inside the bibliography's own group, so the
%% declaration is scoped to the bibliography and reaches nothing after it.
\cs_if_exist:NT \CDossierPlainLinks
  { \AtBeginBibliography { \CDossierPlainLinks } }

%% Use the CV's semantic section command for the bibliography heading when the
%% package is loaded with careerdossier-cv. Other document classes retain
%% BibLaTeX's own default heading and no unrelated heading is redefined.
\cs_if_exist:NT \CDossierSection
  { \defbibheading { bibliography } [ \refname ] { \CDossierSection {#1} } }

%% ---------------------------------------------------------------------------
%% Preferred-author emphasis.
%%
%% Store repeatable exact family/given pairs. BibLaTeX exposes the complete
%% parsed name parts while formatting both full names and initials, so matching
%% those parts avoids the prototype's slash parsing and pgffor dependency.
%% ---------------------------------------------------------------------------
\seq_new:N  \g__cdossier_biblatex_highlight_authors_seq
\tl_new:N   \l__cdossier_biblatex_highlight_family_tl
\tl_new:N   \l__cdossier_biblatex_highlight_given_tl
\bool_new:N \l__cdossier_biblatex_highlight_bool

\msg_new:nnnn { careerdossier-biblatex } { missing-highlight-key }
  { \c_backslash_str CDossierHighlightAuthor~requires~a~nonblank~'#1'~key. }
  {
    Declare~an~exact~BibLaTeX-parsed~name,~for~example: \\
    \c_backslash_str CDossierHighlightAuthor
    \c_left_brace_str family~=~Sadeghi,~given~=~Amir\c_right_brace_str .
  }
\msg_new:nnnn { careerdossier-biblatex } { unknown-highlight-key }
  { Unknown~\c_backslash_str CDossierHighlightAuthor~key~'#1'. }
  { The~supported~keys~are:~family,~given. }

\keys_define:nn { cdossier / biblatex / highlight }
  {
    family  .tl_set:N = \l__cdossier_biblatex_highlight_family_tl ,
    given   .tl_set:N = \l__cdossier_biblatex_highlight_given_tl ,
    unknown .code:n =
      {
        \msg_error:nnx { careerdossier-biblatex }
          { unknown-highlight-key } { \l_keys_key_str }
      } ,
  }

\NewDocumentCommand \CDossierHighlightAuthor { m }
  {
    \tl_clear:N \l__cdossier_biblatex_highlight_family_tl
    \tl_clear:N \l__cdossier_biblatex_highlight_given_tl
    \keys_set:nn { cdossier / biblatex / highlight } {#1}
    \tl_trim_spaces:N \l__cdossier_biblatex_highlight_family_tl
    \tl_trim_spaces:N \l__cdossier_biblatex_highlight_given_tl
    \tl_if_blank:VT \l__cdossier_biblatex_highlight_family_tl
      {
        \msg_error:nnn { careerdossier-biblatex }
          { missing-highlight-key } { family }
      }
    \tl_if_blank:VT \l__cdossier_biblatex_highlight_given_tl
      {
        \msg_error:nnn { careerdossier-biblatex }
          { missing-highlight-key } { given }
      }
    \bool_lazy_and:nnT
      { ! \tl_if_blank_p:V \l__cdossier_biblatex_highlight_family_tl }
      { ! \tl_if_blank_p:V \l__cdossier_biblatex_highlight_given_tl }
      {
        \seq_gput_right:Nx \g__cdossier_biblatex_highlight_authors_seq
          {
            { \exp_not:V \l__cdossier_biblatex_highlight_family_tl }
            { \exp_not:V \l__cdossier_biblatex_highlight_given_tl }
          }
      }
  }

\cs_new_protected:Npn \__cdossier_biblatex_highlight_candidate:nn #1#2
  {
    \str_if_eq:eeT { \namepartfamily } {#1}
      {
        \str_if_eq:eeT { \namepartgiven } {#2}
          {
            \bool_set_true:N \l__cdossier_biblatex_highlight_bool
            \seq_map_break:
          }
      }
  }

\prg_new_protected_conditional:Npnn \__cdossier_biblatex_highlight_if: { T , F , TF }
  {
    \bool_set_false:N \l__cdossier_biblatex_highlight_bool
    \seq_map_inline:Nn \g__cdossier_biblatex_highlight_authors_seq
      { \__cdossier_biblatex_highlight_candidate:nn ##1 }
    \bool_if:NTF \l__cdossier_biblatex_highlight_bool
      { \prg_return_true: }
      { \prg_return_false: }
  }

%% Wrap the name-part formatters, but bold only while BibLaTeX is rendering a
%% bibliography. Numeric citations therefore retain their ordinary formatting,
%% including author names produced by \textcite outside a bibliography.
\cs_new_eq:NN \__cdossier_biblatex_saved_family:n \mkbibnamefamily
\cs_new_eq:NN \__cdossier_biblatex_saved_given:n  \mkbibnamegiven

\renewcommand* \mkbibnamefamily [1]
  {
    \iftoggle { blx@bibliography }
      {
        \__cdossier_biblatex_highlight_if:TF
          { \mkbibbold { \__cdossier_biblatex_saved_family:n {#1} } }
          { \__cdossier_biblatex_saved_family:n {#1} }
      }
      { \__cdossier_biblatex_saved_family:n {#1} }
  }
\renewcommand* \mkbibnamegiven [1]
  {
    \iftoggle { blx@bibliography }
      {
        \__cdossier_biblatex_highlight_if:TF
          { \mkbibbold { \__cdossier_biblatex_saved_given:n {#1} } }
          { \__cdossier_biblatex_saved_given:n {#1} }
      }
      { \__cdossier_biblatex_saved_given:n {#1} }
  }

\endinput
