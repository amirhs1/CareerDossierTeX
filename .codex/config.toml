# Codex-specific settings only.
#
# Shared instructions live in AGENTS.md, which Codex reads from the repository
# root. Skills live in .agents/skills/, which Codex already scans. Nothing
# agent-neutral belongs in this file.
#
# See AI-POLICY.md, "Agent instruction structure", for the layout.

sandbox_mode = "workspace-write"
